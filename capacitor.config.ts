import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'DME_SYSTEM_TAB',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
